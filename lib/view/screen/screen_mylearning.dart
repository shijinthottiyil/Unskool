import 'package:flutter/material.dart';

class ScreenMylearning extends StatelessWidget {
  const ScreenMylearning({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Mylearnig!"),
      ),
    );
  }
}
